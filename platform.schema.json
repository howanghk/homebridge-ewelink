{
    "plugin_alias": "EweLink Max",
    "schema": {
        "type": "object",
        "properties": {
            "platform": {
                "title": "Platform",
                "type": "string",
                "const": "eWeLink",
                "readOnly": true
            },
            "name": {
                "title": "Name",
                "type": "string",
                "const": "eWeLink",
                "readOnly": true
            },
            "countryCode": {
                "title": "The telephone country code linked to your eWeLink account (without the +).",
                "type": "string",
                "required": true
            },
            "phoneNumber": {
                "title": "The phone number linked to your eWeLink account. Leave blank if you use your email instead.",
                "type": "string"
            },
            "email": {
                "title": "The email address linked to your eWeLink account. Leave blank if you use your phone number instead.",
                "type": "string"
            },
            "password": {
                "title": "The password for your eWeLink account.",
                "type": "string",
                "required": true,
            },
            "imei": {
                "title": "IMEI - Only change this if you need to.",
                "type": "string",
                "default": "01234567-89AB-CDEF-0123-456789ABCDEF",
                "required": true,
            },
            
            "groups": {
                "title": "You can group channels of multi-switch devices to simulate another accessory instead of having each switch separately. Currently, only 'blind' (WindowCovering) is supported.",
                "type": "array",
                "items": {
                    "type": "string"
                }
            }
        }
    }
}
